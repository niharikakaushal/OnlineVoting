{
  "name": "bookmyshowtask",
  "description": "Fetch photos from flickr in node.js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "flickrapi": "^0.3.28"
  }
}
